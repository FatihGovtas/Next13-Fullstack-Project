/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "assets.stickpng.com",
            "lh3.googleusercontent.com",
        ]
    }
}

module.exports = nextConfig
